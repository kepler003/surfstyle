import React from 'react';
import Logo from 'atoms/Logo';
import Button from 'atoms/Button';

export default () => (
  <header className="header">
    <Logo />
    <Button shape="icon">H</Button>
  </header>
);